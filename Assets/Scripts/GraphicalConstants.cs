namespace PipelineDreams {
    public static class GraphicalConstants {
        /// <summary>
        /// This indicates how many unit lengths in Unity Scene corresponds to a unit length in the game logic.
        /// </summary>
        public const float WORLDSCALE = 2;
    }
}